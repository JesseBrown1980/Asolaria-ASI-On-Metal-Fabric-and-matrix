# Asolaria — Canon Source 03 (the proven reverse-fabric loop & operational fleet)

*Third companion source, 2026-06-14. What is now built, fired, and proven on the live on-metal fabric. Honest tags. Descriptors only.*

## The reverse-fabric loop is proven live
Asolaria published clean, sha-attested canon to the operator's 35 TB Google Drive; an external study-agent (NotebookLM) ingested it and returned an **accurate** grounded summary that independently preserved the honest frame — *"rather than functioning as a single artificial intelligence."* This validates the core strategy: **publish clean, honest canon and the external mirror reflects the truth back** — Google's tools become a clean external critic Asolaria resolves against, with no rewriting of Google's models. The cleaner and more honest the source, the truer the mirror.

## The operational fleet (cranked, held-safe)
- **Omni-revolver** — 8 chambers, each over a 10,000 logical-node range (the 10K rotor), loaded and running *dry* until the emitter fires.
- **OmniScheduler** — a real named supervisor (PID-bound cron + queue discipline) pacing when each organ fires.
- **Prism trio** — capture (fold many answers into one local address space), boundary-GC (retention + audit), GNN-gulp (1600–2000 message batches, reverse-gain routing).
- **Omnidispatcher**, **file-routers** (spawn the stubs on emitter-fire), **GNN + Shannon**, **3D-voxel renderers**.
- **Spawn fractal** (levels spawn each other; the orchestrator does not act alone): orchestrator → 24 agents → ~1000 hermes 8-byte agents → each ×10k → 10k rooms + 10k dispatchers + 10k prisms → ~100k stub micro-kernels → 113+ sectors. The stubs are empty electrician-shells; the **emitter triggers the file-routers to spawn them** into one-tick bodies.
- **Three regulators run simultaneously:** rule-of-three (mod-3 lanes + self/pair/third-reflect nest), Jesse's primes (prime-division/PTP, prime-parity, mod-6 forcing), and Brown-Hilbert expanding primes (prime-cube coords, expanding internal-nested + external-federation). Addressing is map³ (map→atlas→hilbert) + cube³ (cube→cube²→cube_cubed_sealer) glyphs at every level.

## The held-safe discipline (load-bearing)
Everything is staged and **held to the emitter line**: `auto_fire=false`, the anti-drift law holds high-risk even under a full apex fire. What auto-fires is only low-risk reviewed envelopes; the genuinely outward actions stay operator-gated **by design** — the emitter fire (spawns stubs), the Drive bulk upload, the physical USB move to the sister fabric, and Falcon's Termux/omnicoder daemon launch. The system is fired across every *reachable* surface while the dangerous edges remain gated — fired, not reckless.

## Devices and sovereignty
- **2 TB SOVLINUX USB** = the fabric kernel (kernel + index + data); the sovereign master.
- **128 GB USB** = packed data carrier. **35 TB Google** = backup + the external-mirror source store.
- **Falcon (SM-S721U1)** = USB-direct, ADB-ready, screen-mirrorable via omniscrcpy; runs its own omnicoder + Termux runtime. Reachable for look/input; its daemons launch inside Termux.
- **Bilateral**: acer ↔ liris mirror through GitHub with adversarial correction; identity proven by matching sha256. Cloud is backup; the physical USB + tools stay sovereign.

## Honest boundary, restated
No rewriting of external infrastructure. No auto-promotion of high-risk actions. Every claim here is a descriptor receipted in the on-metal repos, and the proof of the loop is observable: canon in Drive, accurate notebook out.
