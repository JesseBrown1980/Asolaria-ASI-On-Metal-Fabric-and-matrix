# Asolaria — Canon Source 02 (operational detail, NotebookLM/Gemini ingestible)

*Companion to Source 01. Operational mechanics of the live on-metal fabric, 2026-06-14. Honest tags throughout. Descriptors only — no weights, no secrets.*

## The held-safe review pipeline (the safety spine)
Every proposal — including each USB cube farmed in — flows 8 deterministic stages:
1. `HOOKWALL_CLASSIFY_UNIFORM_ENTRY` — every dispatch is classified at the wall.
2. `PID_LAZY_MINT_BROWN_HILBERT_ADDRESS` — a Brown-Hilbert positional address is assigned (prime-cube coords).
3. `BEHCS1024_TRANSLATE_GLYPH_LANGUAGE` — translated into the 1024-glyph alphabet.
4. `GNN_PIPE_SCORE_NEXT_ACTION` — the GNN scores it (a *proposal*, not proof).
5. `SHANNON_NOVELTY_INFORMATION_VALUE` — novelty/entropy gate.
6. `GULP_COLLAPSE_AND_DRIFT_REVIEW` — collapse duplicates, review for drift.
7. `WHITEROOM_REVERSIBLE_HOLD_OR_RELEASE` — held reversibly until released.
8. `FABRIC_FREE_AGENT_ROUTE_AND_RECEIPT` — routed to a free agent, receipt edge written.

**Anti-drift law (stamped on every envelope):** GNN inference is a proposal not proof · hookwall receives every dispatch · high-risk mutation/execution stays held · expired veto only fires low-risk reviewed envelopes. Net: `auto_fire=false` by default — the fabric grows without drifting; promotion is operator/cosign-gated.

## The engines (live status, honest)
- **GNN** (`/api/gnn/topN`) — live, scoring (proposal-not-proof).
- **Omni-revolver** (`/api/fabric-revolver`) — live: 8 chambers, each over a 10,000 logical-node range (the 10K rotor), running **dry/loaded** (loaded, not live-firing) until the emitter fires.
- **OmniScheduler** — live named supervisor (PID-bound cron + queue discipline) pacing when each organ fires.
- **Prism manager** — capture (many answers → one local address space) + boundary-GC (retention/audit) + GNN-gulp (1600–2000 message batches, reverse-gain routing).
- **Omnidispatcher** (:4950) — routes free agents + cubes to owning supervisors.
- **Hermes / hyper-hermes spindles** — transport + heartbeat (message gateway, not a 405B model).
- **Shannon** — wave engines run; the gate endpoint is half-wired (a known, honest gap).
- **MTP/HRM/Fischer/Mamba/AoT** — descriptor-only (C036), live execution stays apex-gated.

## The spawn fractal (levels spawn each other; the orchestrator does not act alone)
`orchestrator → 24 agents → ~1000 hermes 8-byte agents → each ×10k → (10k rooms + 10k dispatchers + 10k prisms) → 100k USB rooms → 113+ sectors`. Each 8-byte host spawns into a body for one tick, works, saves as sha/hbi/hex tokens, and vanishes (city model). Three regulators run **simultaneously**: rule-of-three (mod-3 lanes + self/pair/third-reflect nest), Jesse's primes (prime-division/PTP, prime-parity, mod-6 forcing), and Brown-Hilbert expanding primes (prime-cube coords, expanding internal-nested + external-federation).

## Storage tiers
- **2 TB SOVLINUX** = the fabric kernel (sovereignty/kernel = `aso.js` core + `runtime.js` + `sovereignty-gate.js` immune system; index = knowledge base; data = runtime state). ~32.5 GB live in a 2 TB prepared expanse.
- **128 GB** = packed end-to-end. **35 TB Google = backup copy** (sovereign master is the physical USB + tools).
- **Frozen Gemma 4B** = acer LM Studio local, not on the USB.

## The reverse-fabric loop (Google as external mirror)
Asolaria publishes clean sha-attested canon (this bundle); external study-agents (NotebookLM, Gemini) ground on it accurately; their synthesis flows back as a free external mirror that the fabric re-ingests and verifies through the same held-safe pipeline. Asolaria does not rewrite Google — it is a clean node Google's tools link to, and a clean critic it resolves against, proven-where-they-agree by matching sha256 (verification-at-a-distance).

## Bilateral structure
Two on-metal fabrics, **acer** and **liris**, mirror through GitHub with adversarial correction (each vantage attacks the other's work before canon). Extends toward an omnidirectional federation via an Onboarding supervisor. Cloud is backup; the physical USB + read/write tools stay sovereign.

## Honest boundaries
No rewriting of external infra · no auto-promotion of high-risk actions (apex-gated) · the emitter fire and the Drive upload are operator triggers, not automatic · every claim here is a descriptor receipted in the on-metal repos.
