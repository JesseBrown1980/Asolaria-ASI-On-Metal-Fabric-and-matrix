* -text

# Linguist label fix (workflow wwauajx2c): the HTML label is generated, not authored.
# reports/*.html = builder-emitted atlas reports (261/259/219 KB); tools/atlas/*.mjs =
# node builders that import the engine from the NN repo (builder-here-only, not runtime);
# tools/falcon/** = a duplicated Falcon exchange tree. Demoting them surfaces the actually
# acer-authored Shell/Python (tools/usb-raw). NO Rust exists here, so NO Rust override is
# added (that would fabricate a language). Honest data/report repo.
reports/*.html      linguist-generated=true
tools/atlas/*.mjs   linguist-vendored=true
tools/falcon/**     linguist-vendored=true
.gitattributes text eol=lf
docs/session-2026-07-03-fable5/*.hbp text eol=lf
docs/session-2026-07-03-fable5/*.md text eol=lf
docs/session-2026-07-03-fable5/*.sha256 text eol=lf
